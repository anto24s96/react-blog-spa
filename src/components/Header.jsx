import Navbar from "./Navbar";

export default function () {
    return (
        <header className="h-20 bg-[#b52c0a] flex items-center justify-center sticky top-0 z-50">
            <Navbar />
        </header>
    );
}
